# 缓存参数调优

## 推荐起点
```ts
import { DatabaseConfig } from 'ocorm'

const config = new DatabaseConfig('app.db')
  .setQueryCache(true, 200, 60000)
```

## 调参方法
- 命中率低：缩短 TTL 或减少缓存范围
- 内存占用高：降低容量
- 数据一致性敏感：关键写路径后主动失效
