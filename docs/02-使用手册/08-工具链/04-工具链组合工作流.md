# 工具链组合工作流

推荐顺序：
1. 用 `defineFactory` 定义数据模板
2. 用 `SeederManager` 准备演练数据
3. 用 `MigrationGenerator` 生成迁移骨架
4. 审核并执行迁移

## 典型发布流程
- 开发阶段：Factory + Seeder 快速验证
- 发布前：生成迁移并人工审核
- 发布时：执行迁移 + 最小回归验证

## 注意事项
- 不要把开发演示数据直接用于生产
- 迁移执行失败时先停止写流量
- 每次发布保留迁移记录和执行日志
