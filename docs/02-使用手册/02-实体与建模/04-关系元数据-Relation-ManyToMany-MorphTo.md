# 关系元数据（Relation / ManyToMany / MorphTo）

## 何时用
业务实体之间存在一对多、多对多或多态关联时。

## 怎么用
先明确主从关系与外键，再定义关系加载行为。

关系建模建议：
- 先确定外键字段名。
- 再定义关系名称和方向。
- 查询阶段配合 `with` / `withCount` 使用。

## 常见误用
关系字段命名与外键命名不一致，导致关联加载为空。
