# Unique索引映射规则

唯一约束直接影响写入正确性，应作为迁移检查重点。

## 规则建议
- 在实体定义中显式声明唯一列或唯一组合
- 同一语义只保留一套唯一索引，避免重复索引
- 索引命名保持稳定，减少跨环境漂移

## 常见问题
- 新增唯一后历史脏数据导致迁移失败
- 相同索引重复创建导致发布报错
- 组合唯一字段顺序不一致导致性能退化

## 处理建议
- 加唯一前先清洗数据
- 发布前做一次索引一致性检查
- 将索引规则写入变更说明与回滚方案
