# 2026-03-17-代码审查报告

> 状态：已归档
> 对齐版本：`ocorm 3.0.1`
> 归档时仓库版本：`ocorm 3.0.2`
> 最后更新：2026-03-28

## 1. 审查背景
本报告归档的是 2026-03-17 这一批次在当前仓库中仍可追溯的审查事实。它不是测试执行记录，也不是发布成功证明；它只说明当前代码库里能找到哪些修复描述、回归命名和后续加固轨迹。

按 `OCORM/CHANGELOG.md` 的版本记录，`3.0.1` 这一版的审查焦点集中在三类问题：
- 实体查询路径拒绝 `selectRaw/groupBy/having`，避免静默忽略导致语义偏差
- `rawQuery` 校验收紧，降低注释和字符串片段造成的误判或绕过
- 新增 `QueryExecutor.getRaw()`，把原生 SELECT/聚合/分组查询和实体查询显式分层

```text
本报告的治理边界:
1. 只记录仓库中能回指到文件的事实
2. 允许写“已记录”“已纳入修复批次”“后续继续加固”
3. 不写“已验证通过”，因为仓库里没有本次审查对应的执行产物
```

## 2. 本次证据面
本报告只使用下列真实材料：
- `OCORM/CHANGELOG.md`
- `entry/src/main/ets/suites/EntryOrmReviewReport20FixSuite.ets`
- `entry/src/test/EntryOrmReviewReport20Fix.test.ets`
- `entry/src/main/ets/suites/index.ets`
- `entry/src/test/List.test.ets`
- `OCORM/docs/开发者文档/06-仓储与事务/06-rawQuery-rawQuerySafe-rawExecute.md`
- `OCORM/docs/开发者文档/05-查询体系/06-QueryExecutor原生模式-getRaw.md`

```text
3.0.1 版本记录（来自 CHANGELOG 的可追溯事实）:
- 实体查询路径明确拒绝 selectRaw/groupBy/having
- rawQuery 校验剔除注释与字符串内容
- 新增 QueryExecutor.getRaw()
- entry/src/test/EntryOrmReviewReport20Fix.test.ets 新增实体查询拒绝 / getRaw / rawQuery 校验回归
```

```text
当前仓库中的登记状态:
- entry/src/main/ets/suites/index.ets 已注册 EntryOrmReviewFixSuite
- entry/src/main/ets/suites/index.ets 已注册 EntryOrmReviewReport19FixSuite
- entry/src/main/ets/suites/index.ets 已注册 EntryOrmReviewReport20FixSuite
- entry/src/test/List.test.ets 当前显式导入的 review 相关 Hypium 入口只看到 EntryOrmReviewReport20Fix.test.ets
```

## 3. 发现项
**R-2026-03-17-01 查询语义收紧已被纳入代码与回归命名**

- 级别：`HIGH`
- 现状：`3.0.1` 明确收紧实体查询语义，实体查询路径不再接受 `selectRaw/groupBy/having`，并要求此类场景改走 `getRaw()` 或聚合路径。
- 仓库证据：`OCORM/CHANGELOG.md` 的 `3.0.1` 条目；`entry/src/main/ets/suites/EntryOrmReviewReport20FixSuite.ets` 中的 `createEntityQuery_ShouldRejectSelectRawInGet`、`createEntityQuery_ShouldRejectGroupByInGetPaginated`、`createGetRaw_ShouldReturnResultSetRowArray`；`entry/src/test/EntryOrmReviewReport20Fix.test.ets` 的同名 Hypium 用例。
- 治理判断：当前仓库可以证明这条审查结论已经被写进代码和回归命名；不能证明 2026-03-17 当日已经跑通。

**R-2026-03-17-02 原生 SQL 守卫在后续版本继续加固，不应把 3.0.1 写成终局**

- 级别：`HIGH`
- 现状：`3.0.1` 记录的是 `rawQuery` 校验剔除注释与字符串内容，用于降低误判和绕过；`3.0.2` 又继续把 `rawQuery` 改为默认强制参数化，并补充 `rawQuerySafe` 严格参数化回归。
- 仓库证据：`OCORM/CHANGELOG.md` 的 `3.0.1` 与 `3.0.2` 条目；`EntryOrmReviewReport20FixSuite.ets` 中 `createRawQuery_ShouldRequireParameterizedSql` 与 `createRawQuerySafe_ShouldRequireParameterizedSql`；`06-rawQuery-rawQuerySafe-rawExecute.md` 对参数化边界与失败语义的说明。
- 治理判断：2026-03-17 这批问题应归档为“已记录并已有修复批次，且后续继续加固”，不能写成“3.0.1 已一次性彻底解决所有 raw SQL 风险”。

**R-2026-03-17-03 当前归档线索与测试入口命名存在追踪偏移**

- 级别：`MEDIUM`
- 现状：`3.0.1` 的 changelog 将回归定位到 `EntryOrmReviewReport20Fix.test.ets`，而不是日期同名或 `Report17` 命名文件；同时 `entry/src/main/ets/suites/index.ets` 注册了多条 review 修复线，但 `entry/src/test/List.test.ets` 当前只显式挂载 `EntryOrmReviewReport20Fix.test.ets`。
- 仓库证据：`OCORM/CHANGELOG.md`、`entry/src/main/ets/suites/index.ets`、`entry/src/test/List.test.ets`
- 治理判断：当前仓库能证明“3.0.1 审查批次的核心查询/原生 SQL 回归已被纳入 report20 命名线”，但不能把这种命名收口直接等价为“所有 review 修复线均已通过统一入口执行”。

```text
当前能直接对上 3.0.1 的 suite / test 主题:
- EntityQuery_ShouldRejectSelectRawInGet
- EntityQuery_ShouldRejectGroupByInGetPaginated
- GetRaw_ShouldReturnResultSetRowArray
- RawQuery_ShouldRequireParameterizedSql
- RawQuerySafe_ShouldRequireParameterizedSql   // 该项属于后续 3.0.2 加固
```

## 4. 修复轨迹与当前落点

| 审查主题 | 2026-03-17 / 3.0.1 可追溯事实 | 当前仓库中的后续落点 |
| --- | --- | --- |
| 实体查询语义收紧 | `CHANGELOG` 记录实体查询拒绝 `selectRaw/groupBy/having` | `EntryOrmReviewReport20FixSuite.ets` 与 `EntryOrmReviewReport20Fix.test.ets` 保留拒绝回归命名 |
| 原生结果读取分层 | `CHANGELOG` 记录新增 `QueryExecutor.getRaw()` | `06-QueryExecutor原生模式-getRaw.md` 说明 `ResultSetRow[]` 语义和误用边界 |
| `rawQuery` 风险控制 | `CHANGELOG` 记录注释/字符串剔除校验 | `3.0.2` 再次加固为默认强制参数化，并补 `rawQuerySafe` 严格参数化回归 |
| 测试纳管 | `CHANGELOG` 直接点名 `EntryOrmReviewReport20Fix.test.ets` | `entry/src/main/ets/suites/index.ets` 已注册 suite；`entry/src/test/List.test.ets` 当前只显式导入 report20 测试入口 |

```text
可确认的修复延续:
2026-03-17 -> 3.0.1 首次明确收紧查询语义与 rawQuery 校验
2026-03-27 -> 3.0.2 继续把 rawQuery/rawQuerySafe 收紧到默认强制参数化
结论       -> 这是一条连续治理线，不是单次修复后就静止
```

## 5. 当前不能证明的事项
- 当前仓库没有 2026-03-17 当日的测试执行日志、CI 记录或发布验收产物，因此不能写“已验证通过”。
- 当前仓库没有单独命名为 `EntryOrmReviewReport17Fix` 的套件或测试文件，因此不能把日期报告和文件命名做一一映射。
- `entry/src/test/List.test.ets` 只显式挂载 `EntryOrmReviewReport20Fix.test.ets`，因此不能把 `entry/src/main/ets/suites/index.ets` 中所有 review 线的 suite 注册视为同一入口下已执行闭环。

## 6. 归档结论

```text
结论类型: 已记录并已有修复批次
版本落点: 3.0.1
后续状态: 在 3.0.2 继续加固
仓库可证事实:
- 3.0.1 的查询语义收紧、getRaw 引入、rawQuery 校验收紧已写入 CHANGELOG
- 对应回归主题当前可在 EntryOrmReviewReport20FixSuite / .test 中定位
当前不可证事实:
- 2026-03-17 当日执行通过
- 所有 review 修复线均通过统一入口完成回归
```

本报告最终结论为：`2026-03-17` 这一批查询与原生 SQL 审查问题，在当前仓库中属于“已记录并已有修复批次”的状态；同时，这条治理线在 `3.0.2` 仍发生了强语义加固，因此不应把 `3.0.1` 归档写成终局性修复。

## 7. 引用路径
- `OCORM/CHANGELOG.md`
- `entry/src/main/ets/suites/EntryOrmReviewReport20FixSuite.ets`
- `entry/src/test/EntryOrmReviewReport20Fix.test.ets`
- `entry/src/main/ets/suites/index.ets`
- `entry/src/test/List.test.ets`
- `OCORM/docs/开发者文档/06-仓储与事务/06-rawQuery-rawQuerySafe-rawExecute.md`
- `OCORM/docs/开发者文档/05-查询体系/06-QueryExecutor原生模式-getRaw.md`

## 8. 变更记录
- 2026-03-28：基于当前仓库可追溯材料补全 2026-03-17 审查归档，明确修复延续与不可证事项
