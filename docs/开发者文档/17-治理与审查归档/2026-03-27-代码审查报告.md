# 2026-03-27-代码审查报告

> 状态：已归档
> 对齐版本：`ocorm 3.0.2`
> 最后更新：2026-03-28

## 1. 审查背景
本报告归档的是当前仓库中，围绕 `3.0.2` 可直接追溯到的治理与审查事实。归档目标不是证明“当天全部测试已执行通过”，而是把代码、文档、changelog、suite 命名之间的对应关系固定下来。

本批次从仓库里能明确看到的重点主题有三条：
- `rawQuery` 默认强制参数化
- `DatabaseManager` 测试桥接迁移到 `DatabaseManagerTestHooks`
- 审查与修复线继续收敛到 `EntryOrmReviewReport20Fix*`、`EntryOrmSecurityFix*` 等命名

```text
归档口径:
能证明“已写入代码 / 已写入 suite / 已写入 changelog”
不能证明“2026-03-27 当天已经实际执行通过”
```

## 2. 本次证据面
- `OCORM/CHANGELOG.md`
- `entry/src/main/ets/suites/EntryOrmReviewReport20FixSuite.ets`
- `entry/src/test/EntryOrmReviewReport20Fix.test.ets`
- `entry/src/main/ets/suites/EntryOrmSecurityFixSuite.ets`
- `entry/src/test/EntryOrmSecurityFix.test.ets`
- `entry/src/main/ets/suites/index.ets`
- `entry/src/test/List.test.ets`
- `OCORM/docs/开发者文档/06-仓储与事务/06-rawQuery-rawQuerySafe-rawExecute.md`
- `OCORM/docs/开发者文档/12-安全基线/01-SQL注入防护策略.md`

```text
CHANGELOG 中 3.0.2 的可追溯重点:
- rawQuery 改为默认强制参数化
- BuildProfile 版本常量与构建配置对齐
- DatabaseManager 测试注入 API 迁移到 DatabaseManagerTestHooks
- EntryOrmReviewReport20FixSuite / .test 同步增强 rawQuery / rawQuerySafe 回归
```

## 3. 发现项
**R-2026-03-27-01 原生 SQL 风险控制在 3.0.2 进入 fail-fast 阶段**

- 级别：`HIGH`
- 现状：`rawQuery` 不再只是“只读校验”，而是默认要求参数化和占位符数量严格匹配。
- 证据：`OCORM/CHANGELOG.md`；`EntryOrmReviewReport20FixSuite.ets`；`EntryOrmReviewReport20Fix.test.ets`；`06-rawQuery-rawQuerySafe-rawExecute.md`
- 归档结论：已记录并已有修复批次。

**R-2026-03-27-02 测试桥接入口已迁移，但调用方必须同步迁移**

- 级别：`MEDIUM`
- 现状：`CHANGELOG` 明确说明测试注入 API 已迁移到 `database/testing/DatabaseManagerTestHooks`。
- 证据：`OCORM/CHANGELOG.md`；当前 `DatabaseManagerTestHooks.ets`；升级文档 `15-升级与兼容/02-3.0.1-3.0.2行为变化.md`
- 归档结论：已记录并已有修复批次，但测试代码若继续依赖旧桥接路径会产生兼容性问题。

**R-2026-03-27-03 审查线命名和入口登记仍需按“可证事实”描述**

- 级别：`MEDIUM`
- 现状：`entry/src/main/ets/suites/index.ets` 中存在多条修复线注册，但 `entry/src/test/List.test.ets` 的显式 Hypium 入口不应被自动解读成“所有修复线已统一执行”。
- 证据：`entry/src/main/ets/suites/index.ets`；`entry/src/test/List.test.ets`
- 归档结论：已记录但入口覆盖仍需复核。

```text
本报告中允许的结论:
- 已记录并已有修复批次
- 已记录但入口覆盖仍需复核
不允许的结论:
- 全量执行通过
- 所有 review/security 线均已闭环
```

## 4. 修复轨迹与当前落点

| 主题 | 当前仓库可证事实 | 归档判断 |
| --- | --- | --- |
| `rawQuery` 参数化收紧 | `CHANGELOG 3.0.2` + `ReviewReport20Fix` suite/test + 当前文档 | 已记录并已有修复批次 |
| 测试桥接迁移 | `CHANGELOG 3.0.2` + `DatabaseManagerTestHooks.ets` | 已记录并已有修复批次 |
| 安全基线沉淀 | `12-安全基线/01-SQL注入防护策略.md` | 已记录 |
| 入口登记一致性 | `suites/index.ets` 与 `test/List.test.ets` 需要同时核对 | 已记录但入口覆盖仍需复核 |

## 5. 当前不可证事项
- 当前仓库没有 2026-03-27 当日执行日志，因此不能写“已验证通过”。
- 仅凭 suite 命名存在，不能证明所有对应 case 都被当前入口执行。
- 仅凭 changelog 描述，不能证明外部项目升级时零改动。

```text
不可证事项必须保留:
1. 当日执行结果
2. 全量入口覆盖
3. 外部项目升级零迁移成本
```

## 6. 归档结论

```text
结论类型: 已记录并已有修复批次
适用版本: 3.0.2
仓库证据:
- CHANGELOG 已记录
- Review/Security 相关 suite/test 命名已存在
- 开发者文档已补齐行为边界
保留风险:
- 执行记录缺失时不得写“已验证通过”
- 入口挂载与 suite 注册仍需一起看
```

本报告最终结论为：`2026-03-27` 这一批治理问题在当前仓库中已经有清晰的修复与文档落点，但归档仍必须保持“代码已纳管、执行结果待证据”的保守表达。

## 7. 引用路径
- `OCORM/CHANGELOG.md`
- `entry/src/main/ets/suites/index.ets`
- `entry/src/test/List.test.ets`
- `entry/src/main/ets/suites/EntryOrmReviewReport20FixSuite.ets`
- `entry/src/test/EntryOrmReviewReport20Fix.test.ets`
- `entry/src/main/ets/suites/EntryOrmSecurityFixSuite.ets`
- `entry/src/test/EntryOrmSecurityFix.test.ets`
- `OCORM/docs/开发者文档/06-仓储与事务/06-rawQuery-rawQuerySafe-rawExecute.md`
- `OCORM/docs/开发者文档/12-安全基线/01-SQL注入防护策略.md`

## 8. 变更记录
- 2026-03-28：基于当前仓库可追溯材料补全 2026-03-27 审查归档
