import { describe, it, expect } from '@ohos/hypium'

export default function smokeTest() {
  describe('ocormSmokeTest', () => {
    it('should run unit test suite', 0, () => {
      expect(true).assertTrue()
    })
  })
}
